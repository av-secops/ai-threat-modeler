# Optional Aegis Threat 2.3.2 GPU QLoRA training dependencies.
# Use a separate GPU environment. Embedding training dependencies are in requirements.txt.
-r requirements.txt
bitsandbytes>=0.45.0,<1.0.0
peft>=0.14.0,<1.0.0
trl>=1.12.0,<2.0.0
